<?xml version="1.0" encoding="UTF-8" ?>
<Frame Margin="1,1" CornerRadius="5"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Notes.CustomMarkupXtension"
    x:Class="Notes.Views.TextCView" BorderColor="Gray">
     
    <StackLayout Orientation="Horizontal">
        <Image HeightRequest="60"
          Source="{local:ImageResourceX Notes.Images.Text.png}"/>

        <StackLayout VerticalOptions="CenterAndExpand">
                <Label Text="{Binding Title}" Style="{DynamicResource PrimaryLabelStyle}"/>
                <Label Text="{Binding CreatedAt}" Style="{DynamicResource PrimaryLabelStyle}"/>
        </StackLayout>
    </StackLayout>
</Frame>

